name: Expressions & Context
on: [push,pull_request,issues, workflow_dispatch]
jobs:

    using-expressions_and_contexts:
        runs-on: ubuntu-latest
        steps:
            - name: Expressions
              id: expressions
              run: |
               echo ${{1}}
               echo ${{'This is a string'}}
               echo ${{null}}
               echo ${{true}}
               echo ${{1>2}}
               echo ${{'string' == 'String'}}
               echo ${{true && false}}
               echo ${{true || (false && true)}}
            - name: Dummp Contexts
              run: | 
               echo ${{toJson(github)}}
               echo ${{toJson(job)}}
               echo ${{toJson(secrets)}}
               echo ${{toJson(steps)}}
               echo ${{toJson(runner)}}            
